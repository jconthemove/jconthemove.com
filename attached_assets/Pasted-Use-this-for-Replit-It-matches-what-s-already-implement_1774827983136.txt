Use this for Replit. It matches what’s already implemented here and corrects the file list to your actual repo structure.

Replit Handoff
Task: Merge Calculator + Guided Quote Entry

Implement a unified homepage booking section so customers choose between two equal-weight paths before starting:

Instant Pricing
Guided Questions
What changed

Replaced the separate homepage calculator/form split with one shared “Get Started” section.
Added tab-style selection with equal visual weight.
Instant Pricing renders the existing live calculator.
Guided Questions renders the booking chatbot embedded inline.
Removed the floating chatbot launcher from the homepage to avoid duplicate guided entry points.
Kept the manual fallback quote form lower on the page for custom jobs.
Done looks like

Homepage shows one unified quote/booking section.
Tabs say:
Instant Pricing — “Build your move and see pricing now.”
Guided Questions — “Not sure what you need? We’ll walk you through it.”
Switching tabs swaps content inline without breaking either flow.
Embedded chatbot has no dialog-only chrome and no close button.
CTA buttons and service shortcuts scroll to the unified section.
Residential shortcuts preselect calculator.
Non-calculator-friendly services can default to guided.
Manual quote form still exists lower on the page.
No pricing/backend logic changes.
Files to update

client/src/pages/home.tsx
client/src/components/booking-chatbot.tsx
Note

client/src/components/homepage-booking-calculator.tsx is reused, not redesigned.
client/src/pages/customer/book.tsx does not exist in this repo, so it is not part of this change.
Implementation details

In client/src/components/booking-chatbot.tsx, add:

embedded?: boolean
showCloseButton?: boolean
className?: string
In embedded mode:

render inline container styles
hide close button by default
keep question flow and submission logic unchanged
In client/src/pages/home.tsx:

add entryMode state: "calculator" | "guided"
create one shared section with tab buttons
render HomepageBookingCalculator when entryMode === "calculator"
render BookingChatbot embedded when entryMode === "guided"
update scrollToQuote(...) to set the tab before scrolling
remove the old floating chatbot dialog/launcher
keep the manual quote form below as fallback
Already verified locally

npm run build passed successfully.
If you want, I can also turn this into a shorter “paste-this-into-Replit” prompt with no extra explanation.